@model IEnumerable<EmployeeDTO>

<h1>@ViewBag.Title</h1>

<div class="container">
    <a asp-controller="employee" asp-action="create">Create New</a>
    <div class="card-columns">
        @foreach (var employee in Model)
        {

            <div class="card card-user">
                <img class="card-img-top" src="~/img/user.png" alt="Card image" style="width:100%">
                <div class="card-body">
                    <h4 class="card-title">@employee.Name</h4>
                    <a asp-controller="employee" asp-action="details" asp-route-id="@employee.Id">See Profile</a>
                </div>
            </div>
        }
    </div>
</div>



